#ifndef INCLUDED_COM_SUN_STAR_SHEET_FILLDIRECTION_HDL
#define INCLUDED_COM_SUN_STAR_SHEET_FILLDIRECTION_HDL

#include "sal/config.h"

#include "cppu/macros.hxx"

namespace com { namespace sun { namespace star { namespace uno { class Type; } } } }

namespace com { namespace sun { namespace star { namespace sheet {

#if defined LIBO_INTERNAL_ONLY

#if defined __GNUC__

enum class FillDirection
{

#else

enum class SAL_DLLPUBLIC_RTTI FillDirection
{

#endif

#else

enum SAL_DLLPUBLIC_RTTI FillDirection
{

#endif
    FillDirection_TO_BOTTOM = 0,
    FillDirection_TO_RIGHT = 1,
    FillDirection_TO_TOP = 2,
    FillDirection_TO_LEFT = 3,
    FillDirection_MAKE_FIXED_SIZE = SAL_MAX_ENUM
};

#if defined LIBO_INTERNAL_ONLY
constexpr auto FillDirection_TO_BOTTOM = FillDirection::FillDirection_TO_BOTTOM;
constexpr auto FillDirection_TO_RIGHT = FillDirection::FillDirection_TO_RIGHT;
constexpr auto FillDirection_TO_TOP = FillDirection::FillDirection_TO_TOP;
constexpr auto FillDirection_TO_LEFT = FillDirection::FillDirection_TO_LEFT;
#endif

inline ::css::uno::Type const & cppu_detail_getUnoType(SAL_UNUSED_PARAMETER ::css::sheet::FillDirection const *);
} } } }

SAL_DEPRECATED("use cppu::UnoType") inline ::css::uno::Type const & SAL_CALL getCppuType(const ::css::sheet::FillDirection *);

#endif
