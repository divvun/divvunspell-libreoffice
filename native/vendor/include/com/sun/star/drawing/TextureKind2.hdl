#ifndef INCLUDED_COM_SUN_STAR_DRAWING_TEXTUREKIND2_HDL
#define INCLUDED_COM_SUN_STAR_DRAWING_TEXTUREKIND2_HDL

#include "sal/config.h"

#include "cppu/macros.hxx"

namespace com { namespace sun { namespace star { namespace uno { class Type; } } } }

namespace com { namespace sun { namespace star { namespace drawing {

#if defined LIBO_INTERNAL_ONLY

#if defined __GNUC__

enum class TextureKind2
{

#else

enum class SAL_DLLPUBLIC_RTTI TextureKind2
{

#endif

#else

enum SAL_DLLPUBLIC_RTTI TextureKind2
{

#endif
    TextureKind2_LUMINANCE = 0,
    TextureKind2_INTENSITY = 1,
    TextureKind2_COLOR = 2,
    TextureKind2_MAKE_FIXED_SIZE = SAL_MAX_ENUM
};

#if defined LIBO_INTERNAL_ONLY
constexpr auto TextureKind2_LUMINANCE = TextureKind2::TextureKind2_LUMINANCE;
constexpr auto TextureKind2_INTENSITY = TextureKind2::TextureKind2_INTENSITY;
constexpr auto TextureKind2_COLOR = TextureKind2::TextureKind2_COLOR;
#endif

inline ::css::uno::Type const & cppu_detail_getUnoType(SAL_UNUSED_PARAMETER ::css::drawing::TextureKind2 const *);
} } } }

SAL_DEPRECATED("use cppu::UnoType") inline ::css::uno::Type const & SAL_CALL getCppuType(const ::css::drawing::TextureKind2 *);

#endif
